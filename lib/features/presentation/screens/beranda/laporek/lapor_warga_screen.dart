import 'package:flutter/material.dart';

class LaporWargaScreen extends StatelessWidget {
  const LaporWargaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Lapor Warga Screen')));
  }
}
